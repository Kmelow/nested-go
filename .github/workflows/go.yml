name: hello world

on: [push, pull_request]

jobs:
  Lint:
    runs-on: ubuntu-20.04
    steps:
      - name: Checking working dir
        run: pwd
      - name: Checking folder contents
        run: ls -la
